import { Grid, Typography } from '@mui/material'
import { styled } from '@mui/material/styles'

export const ContentLayout = styled(Grid)({
    marginTop: '6rem',
    padding: '0 2rem',
})

export const HelpText = styled(Typography)({
    textAlign: 'center',
    marginTop: '1.2rem',
})

export const StorySetText = styled(Typography)({
    marginBottom: '1.2rem'
})
